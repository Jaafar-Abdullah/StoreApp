<% @products.each do |product| %>
<% if current_user.id == product.user_id %>
    
  <%= link_to product.name, product_path(product) %>
  <p>Description: <%= product.description %>Î</p>
  <p>Price: <%= product.price %>Î</p>
  <p>------------</p>
<% end %>

<% end %>
